# Evidence — {{ display_name }}

Each resume-safe claim is tied to code, a test, and a reproduce command.
Distinguish implementation evidence from experimental-performance evidence.

| Claim ID | Claim | Source | Test | Reproduce |
| --- | --- | --- | --- | --- |
| C1 | Inputs are validated against a strict typed schema | `src/{{ package_name }}/models.py` | `tests/test_models.py` | `pytest tests/test_models.py` |
| C2 | The agent core runs deterministically end-to-end on the bundled example | `src/{{ package_name }}/agent.py` | `tests/test_agent.py` | `pytest tests/test_agent.py` |
| C3 | Ungrounded / invalid outputs are rejected, not emitted | `src/{{ package_name }}/validators.py` | `tests/test_agent.py` | `pytest -k reject` |

Tool/model version: mock LLM `mock-llm-deterministic-v1` (default). Release tag: `TBD-placeholder`.

## Non-claims
This project does not claim formal signoff, verified assertion generation, or
autonomous proof. See README "Claims policy" (generated from the manifest).
